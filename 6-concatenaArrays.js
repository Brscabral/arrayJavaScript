//Aqui vamos concatenar arrays
//Lembrando que, ao usar a função, precisaremos atribuir a uma variavel ou const
//vamos juntar duas salas de alunos que cursam python e javaScript
const salaJavaScript=['Raphael','Paulo', 'Ana'];
const salaPython=['Leandro', 'Rodrigo','Miguel'];
const unirSalas=salaJavaScript.concat(salaPython);
//o parametro é esse:Pega o nome que vc deu a variavel ou a const, aplica a função e poe o que vc quer
//concatenar dentro do argumento.
console.log(unirSalas);